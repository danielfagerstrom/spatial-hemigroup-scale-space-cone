Review response R3, literature and positioning, the paper-side sentences (the Sato 2001 citation in the copied remark rem:dimension-filtration was made at the blueprint and in the copy by a script, and is not in this spec). Sources read by the librarian from held copies on 2026-09-19: Burgeth, Didas and Weickert, "The Bessel scale-space", sections 2.2 and 2.4, pp. 88-89; the same authors' "Relativistic scale-spaces", pp. 3-7; Sato, Theorem 17.5, pp. 108-109; Lindeberg 1990, the classification of discrete scale-space kernels by their generating functions. Applied with scripts/apply-anchors.py.

@@@ FILE paper-b/01-introduction.tex
@@@ OLD
positivity, and that a smoothing of a smoothing is again a member of the family. Under them
the kernel must be the Gaussian, and the scale space is the solution of the heat equation
@@@ NEW
positivity, that a smoothing of a smoothing is again a member of the family, and a requirement
of locality, in one of its forms: that the evolution in scale is generated by a differential
operator, or that no new structure appears as the scale grows. Under them
the kernel must be the Gaussian, and the scale space is the solution of the heat equation
@@@ END

@@@ FILE paper-b/01-introduction.tex
@@@ OLD
$(1 + t^2\omega^2)^{-\gamma}$, the \Matern{} covariance functions of spatial statistics, which
have every moment finite and which no semigroup axiomatics reaches. In probabilistic terms
@@@ NEW
$(1 + t^2\omega^2)^{-\gamma}$, which for $\gamma > \tfrac12$ are the \Matern{} covariance
functions of spatial statistics. They have every moment finite, and no semigroup that is
covariant under dilation contains them; in the order $\gamma$ at a fixed range they do form
a semigroup, the Bessel scale space discussed below. In probabilistic terms
@@@ END

@@@ FILE paper-b/06-generator.tex
@@@ OLD
$B = \omega F'$ of \ref{eq:symbol}, evaluated at $t\omega$ and divided by $t$. Since
$B \ge 0$, every wavenumber is damped, at the rate $B(t\omega)/t$. The signal side is the same
@@@ NEW
$B = \omega F'$ of \ref{eq:symbol}, evaluated at $t\omega$ and divided by $t$. Since
$B \ge 0$, every wavenumber is damped, at the rate $B(t\omega)/t$. The symbol is a classical
object of the probability literature. A self-decomposable law is the limit law of a process of
Ornstein--Uhlenbeck type, its exponent being $\int_0^\infty \psi(e^{-s}\omega)\,ds$ for the
exponent $\psi$ of a \Levy{} process, the background driving process
(\sscite{sato1999levy}, Theorem~17.5, pp.~108--109), and differentiating that representation
gives $\psi = \omega F' = B$. What this section adds to it is the reading as an evolution
equation for the signal, with its domain and its constants. The signal side is the same
@@@ END

@@@ FILE paper-b/07-implementation.tex
@@@ OLD
periodic signal of the run the kernels are wrapped around the $4096$ samples, which changes
@@@ NEW
lattice the kernels from the origin are convolution powers of the two-sided geometric kernel,
which are among the discrete scale-space kernels of \sscite{lindeberg1990scalespace}, whose
classification allows the factors $1/(1 - \beta z)$ and $1/(1 - \gamma z^{-1})$ in the
generating function. A stage between two positive scales is not among them: it has the
factor $1 - p_sz^{-1}$, a zero on the positive real axis, which that classification excludes.
So the stages of the sampled cascade preserve positivity and mass, which is what this paper's
axioms ask, and not the stronger property of creating no new extrema, which belongs to the
kernels from the origin and is the subject of the next module. On the
periodic signal of the run the kernels are wrapped around the $4096$ samples, which changes
@@@ END

@@@ FILE paper-b/01-introduction.tex
@@@ OLD
composed along the smoothness and not along the range they are the Bessel scale space of
\sscite{burgeth2005bessel}, and in probability they are the variance-gamma laws of
@@@ NEW
composed along the smoothness and not along the range they are the Bessel scale space of
\sscite{burgeth2005bessel}, who also dilate the kernels at a fixed order; what the hemigroup
adds there is the stage between two ranges, which makes the family in the range a cascade. The
same authors' relativistic scale spaces (\sscite{burgeth2005relativistic}), with the
exponent $\sqrt{\omega^2 + m^2} - m$, are a further non-Gaussian semigroup in imaging with
exponentially decaying kernels; their scale parameter is not the dilation of the kernel, so
they are covariant in a different sense from the one required here. In probability the
\Matern{} kernels are the variance-gamma laws of
@@@ END
